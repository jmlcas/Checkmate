services:

  checkmate:
    image: ghcr.io/bluewave-labs/checkmate-backend-mono:latest
    container_name: checkmate
    restart: unless-stopped
    ports:
      - "52345:52345"
    environment:
      - CLIENT_HOST=http://localhost:52345
      - UPTIME_APP_CLIENT_HOST=http://localhost:52345
      - UPTIME_APP_API_BASE_URL=http://localhost:52345/api/v1
      - UPTIME_ALLOWED_ORIGINS=http://localhost:52345
      - UPTIME_APP_PUBLIC_ASSETS_URL=http://localhost:52345/uploads
      - UPTIME_APP_UPLOAD_DIR=/app/uploads
      - JWT_SECRET=my_secret
      - DB_CONNECTION_STRING=mongodb://mongodb:27017/checkmate
    volumes:
      - ./uploads:/app/uploads
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - mongodb

  mongodb:
    image: ghcr.io/bluewave-labs/checkmate-mongo:latest
    container_name: checkmate-mongo
    restart: unless-stopped
    command: ["mongod", "--quiet", "--bind_ip_all"]
    volumes:
      - ./mongo/data:/data/db

