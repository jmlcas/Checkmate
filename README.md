# Checkmate

Si no estás en localhost, cambia localhost por tu IP en el archivo .env

También puedes modificar el valor de JWT_SECRET - Puedes crear una key con este comando: openssl rand -base64 32

Ver en "http://localhost:52345"
